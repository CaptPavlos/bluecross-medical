'use client';

/**
 * Main Dashboard Page
 * 
 * The primary view — shows the MonitorDashboard with all vital sign components.
 */

import { MonitorDashboard } from '@/components/MonitorDashboard';

export default function HomePage() {
  return <MonitorDashboard />;
}
